const acorn = require('acorn'); const fs = require('fs'); const code = fs.readFileSync('tmp/provider_syntax_wrapper4.js', 'utf8'); const it = acorn.tokenizer(code, {ecmaVersion:2024, sourceType:'module', locations:true}); let t; while ((t = it.getToken()).type.label !== 'eof') { if (t.loc.start.line >= 290) { console.log(t.loc.start.line, t.loc.start.column, t.type.label, JSON.stringify(t.value)); if (t.loc.start.line > 310) break; } }
